<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Nodeshop</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active1" aria-current="page" href="/">Products</a>
        </li>
        {{#if login}}
          {{#if user.admin}}
            <li class="nav-item">
              <a class="nav-link active3" href="/admin">Admin</a>
            </li>
          {{else}}
            <li class="nav-item">
              <a class="nav-link active2" href="/basket">Basket
              {{#if session.cart.totalQuantity}}
                <span class="badge">{{session.cart.totalQuantity}}</span>
              {{/if}}
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link active3" href="/user/profile">Account</a>
            </li>
          {{/if}}
          <li class="nav-item">
            <a class="nav-link active4" href="/user/logout">Log out</a>
          </li>

        {{else}}
          <li class="nav-item">
            <a class="nav-link active4" href="/user/signup">Register</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active4" href="/user/signin">Log in</a>
          </li>
        {{/if}}

      </ul>
      <form class="d-flex" action="/search" method="GET">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Search"
          aria-label="Search"
          name="filter"
        />
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>